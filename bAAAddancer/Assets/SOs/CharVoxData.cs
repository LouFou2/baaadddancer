using UnityEngine;

[CreateAssetMenu(fileName = "NewCharVoxData", menuName = "CharVoxData")]
public class CharVoxData : ScriptableObject
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
